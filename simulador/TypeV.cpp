/**
 *Implementación de los métodos de la clase TypeV
 */
#include "TypeV.hpp"

using namespace simulator;

//Almacena la posición de un vértice
void TypeV::readTypeV(float x, float y, float z){
    setX(x);
    setY(y);
    setZ(z);
}
