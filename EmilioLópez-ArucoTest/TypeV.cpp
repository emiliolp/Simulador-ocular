#include "TypeV.hpp"
#include <cmath>
#include <iostream>

using namespace aruco_test;
using namespace std;

void TypeV::readTypeV(float x, float y, float z){
    setX(x);
    setY(y);
    setZ(z);
}
